import 'dart:ui';
import 'package:flutter/material.dart';

class ThemeColors {
  // Light Theme Colors
  static const lightYaziColor = Color(0xFF2D3142); // Koyu lacivert-gri
  static const lightButtonArkaPlan = Color(0xFF4F6D7A); // Deniz mavisi
  static const lightContextBackground = Color(0xFFF8F9FA); // Çok açık gri
  static const lightContextColor = Color(0xFF2D3142); // Koyu lacivert-gri
  static const lightAppBackground = Color(0xFFEDF2F4); // Açık gri-mavi

  // Dark Theme Colors
  static const darkYaziColor = Color(0xFFE0E7EA); // Açık gri-mavi
  static const darkButtonArkaPlan = Color(0xFF9E78E0); // Canlı mor
  static const darkContextBackground = Color(0xFF1F2937); // Koyu lacivert-gri
  static const darkContextColor = Color(0xFFF8F9FA); // Çok açık gri
  static const darkAppBackground = Color(0xFF111827); // Çok koyu lacivert

  // Accent Colors (gerekirse kullanmak için)
  static const accentSuccess = Color(0xFF10B981); // Yeşil
  static const accentWarning = Color(0xFFF59E0B); // Turuncu
  static const accentDanger = Color(0xFFEF4444); // Kırmızı
  static const accentInfo = Color(0xFF3B82F6); // Mavi
}
